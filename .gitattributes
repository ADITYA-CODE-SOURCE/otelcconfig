# Copyright The otelcconfig Authors
# SPDX-License-Identifier: Apache-2.0

# Normalize line endings to LF on every platform. This keeps the committed
# generated/ artifacts byte-stable so golden tests and `generate --check` pass
# on Windows regardless of core.autocrlf.
* text=auto eol=lf